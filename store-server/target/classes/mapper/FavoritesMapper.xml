<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easylife.dao.SecondHandCommunityDao.FavoritesMapper">
    <resultMap id="BaseResultMap" type="com.easylife.pojo.Favorites">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_id" jdbcType="INTEGER" property="user_id"/>
        <result column="idle_id" jdbcType="INTEGER" property="idle_id"/>
    </resultMap>

    <sql id="Base_Column_List">
    id, user_id, idle_id
  </sql>

    <select id="selectFavoritesByUserIdAndIdleId" resultMap="BaseResultMap" parameterType="map">
        SELECT
        <include refid="Base_Column_List"/>
        from community_favorites
        where user_id = #{user_id}
        and idle_id = #{idle_id}
    </select>
    <delete id="deleteByUserIdAndIdleId" parameterType="map">
        delete from community_favorites
        where user_id = #{user_id}
        and idle_id = #{idle_id}
    </delete>
    <select id="selectFavoritesByUserId" resultMap="BaseResultMap" parameterType="map">
        SELECT
        <include refid="Base_Column_List"/>
        from community_favorites
        where user_id = #{user_id}
    </select>

</mapper>